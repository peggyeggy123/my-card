
export interface ServiceItem {
  id: string;
  title: string;
  description: string;
  icon: React.ReactNode;
  color: string;
}

export interface ContactInfo {
  name: string;
  company: string;
  title: string;
  slogan: string;
  phone: string;
  lineId: string;
  instagramId: string;
  email: string;
}
